import { View } from "react-native";
import { Text } from "react-native-paper";

const HomePage = () => {
  return (
    <View>
      <Text>HomePage</Text>
    </View>
  );
};

export default HomePage;
